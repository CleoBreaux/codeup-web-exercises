"use strict";
var myName = 'Corey'
function sayHello(name){
    console.log("Hello " + myName + "!" );
}
 sayHello(" Hello Corey !")

function isTwo(numb) {
    return isTwo(numb)=== 2;
    console.log(isTwo(random))
}

function calculateTip(tipPercent, totalBill) {
    return (tipPercent * totalBill)
}
    console.log(calculateTip( 0.15, 15) );
var userTotal = prompt(number("How much is your total bill ?");
var userTip = prompt(number("How much do you want to tip ?"));
var userbalance = (userTip + userTotal)
console.log(userbalance)
alert("Your total balance is ")

function applyDiscount(price, discount) {
    return console.log(price - (price - discount));
}
applyDiscount(price=, discount=)

